{
  "name": "tree-sitter-hexa",
  "version": "0.1.0",
  "description": "Hexa grammar for tree-sitter",
  "main": "bindings/node",
  "keywords": [
    "parser",
    "hexa",
    "tree-sitter"
  ],
  "scripts": {
    "build": "tree-sitter generate && node-gyp rebuild",
    "test": "tree-sitter test",
    "build-test": "tree-sitter generate && node-gyp build && tree-sitter test"
  },
  "author": "Rileran",
  "license": "MIT",
  "dependencies": {
    "nan": "^2.17.0"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.20.7"
  }
}
